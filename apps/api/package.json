{
  "name": "@orcasynapse/api",
  "version": "9.5.6",
  "description": "OrcaSynapse control-plane API: identity, policy, orchestration, and audit",
  "license": "BUSL-1.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\" && tsc -p tsconfig.build.json",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --exclude=dist/**"
  },
  "dependencies": {
    "@fastify/helmet": "13.1.0",
    "@orcasynapse/contracts": "workspace:*",
    "@orcasynapse/database": "workspace:*",
    "@orcasynapse/runtime-clients": "workspace:*",
    "@orcasynapse/security": "workspace:*",
    "drizzle-orm": "0.45.2",
    "fastify": "5.10.0"
  },
  "devDependencies": {
    "tsx": "4.23.1"
  }
}
